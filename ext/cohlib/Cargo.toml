[package]
name = "cohlib_ext"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "cohlib"
crate-type = ["cdylib"]

# TEMPORARY: pinned to the add-scenario-map-dimensions branch pending
# https://github.com/ryantaylor/cohlib/pull/10. Revert to the bare `git` form
# (no `branch`) once that PR merges and this is re-pinned via `cargo update`.
[dependencies]
cohlib-rb = { git = "https://github.com/ryantaylor/cohlib", branch = "add-scenario-map-dimensions" }
